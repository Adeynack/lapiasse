{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run TUI",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/lapiasse",
            "cwd": "${workspaceFolder}",
            "args": [
                "tui"
            ],
            "env": {
                "GOEXPERIMENT": "jsonv2"
            }
        }
    ]
}