<%# locals: (title: nil) %>
<%#   title:    Optional title, displayed in the card's header. %>
<%#   &block:   Content of the card's body. %>
<details open="true">
  <% if title.present? %>
    <summary>
      <%= title %>
    </summary>
  <% end %>
  <%= yield %>
</details>
